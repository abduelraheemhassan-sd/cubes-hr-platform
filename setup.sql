-- ملف تهيئة قاعدة البيانات
-- يتم تشغيل هذا الملف تلقائياً عند بدء حاوية MySQL

-- استخدام قاعدة البيانات
USE cubes_hr_db;

-- إنشاء جداول المستخدمين (إذا لم تكن موجودة)
-- سيتم إنشاء الجداول تلقائياً بواسطة Drizzle ORM

-- إدراج بيانات تجريبية (اختياري)
-- يمكن حذف هذا الجزء إذا كنت تريد قاعدة بيانات فارغة

-- تعليق: سيتم إنشاء الجداول بواسطة pnpm db:push
-- بعد تشغيل Docker Compose
